const EFF=(()=>{
  const dataUrl='data/courses.json', local=JSON.parse(localStorage.getItem('effAcademyProgress')||'{}');
  const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const load=()=>fetch(dataUrl).then(r=>r.json());
  const complete=(c,l)=>window.EFFAcademyAuth?window.EFFAcademyAuth.complete(c,l):Boolean(local[c]?.includes(l));
  const mark=(c,l)=>window.EFFAcademyAuth?window.EFFAcademyAuth.mark(c,l):(local[c]=[...new Set([...(local[c]||[]),l])],localStorage.setItem('effAcademyProgress',JSON.stringify(local)));
  const stats=course=>({done:course.lessons.filter(l=>complete(course.id,l.id)).length,total:course.lessons.length});
  const strip=markdown=>esc(markdown.replace(/^#.+\n+/,''))
    .replace(/^##\s+(.+)$/gm,'<h2>$1</h2>').replace(/^[-*]\s+(.+)$/gm,'<li>$1</li>')
    .replace(/\n{2,}/g,'</p><p>').replace(/\n/g,'<br>').replace(/<p><li>/g,'<ul><li>').replace(/<\/li><\/p>/g,'</li></ul>');
  const themeFor=title=>{const v=title.toLowerCase();if(/board|meeting|agenda|decision|documentation/.test(v))return['board','Board excellence in action'];if(/conflict|wellness|mental|culture/.test(v))return['conflict','Healthy culture, healthy leaders'];if(/financ|fundrais|treasur|budget|money/.test(v))return['finance','Financial stewardship'];if(/faith|prayer|ministry|spiritual|chaplain|bible/.test(v))return['faith','Faith in leadership'];if(/brand|social|market|communicat|represent/.test(v))return['brand','Representation with purpose'];if(/event|program|workshop|campaign/.test(v))return['events','Campus impact in motion'];if(/member|recruit|induct|sisterhood/.test(v))return['membership','Building belonging'];if(/compliance|report|govern|conduct|policy|operations/.test(v))return['governance','Leading with integrity'];if(/service|community|reach|dropout|ecosystem|resource/.test(v))return['service','Service that changes futures'];return['leadership','Leadership in action']};
  async function start(){await window.EFFAcademyAuth?.hydrate()}
  function renderCatalog(){load().then(async({courses})=>{await start();document.querySelector('#courseCatalog').innerHTML=courses.map((c,i)=>{const s=stats(c);return `<article class="course-card"><div class="number">0${i+1}</div><div><h2>${esc(c.title)}</h2><p>${esc(c.description).slice(0,250)}${c.description.length>250?'…':''}</p><div class="meta"><span>${c.modules.length} learning sections</span><span>${c.lessons.length} lessons</span><span>${s.done}/${s.total} completed</span></div></div><a class="button primary" href="course.html?course=${c.id}">Open pathway →</a></article>`}).join('')}).catch(()=>document.querySelector('#courseCatalog').innerHTML='<p>Course content is loading. Please refresh in a moment.</p>')}
  function renderCourse(){load().then(async({courses})=>{await start();const id=new URLSearchParams(location.search).get('course')||courses[0].id,course=courses.find(c=>c.id===id)||courses[0];let active=course.lessons[0];const root=document.querySelector('#courseRoom');const unlocked=lesson=>{const pos=course.lessons.findIndex(x=>x.id===lesson.id);return pos===0||course.lessons.slice(0,pos).every(x=>complete(course.id,x.id))};const draw=()=>{const groups=course.modules.map(m=>({m,lessons:course.lessons.filter(l=>l.sectionId===m.id).sort((a,b)=>a.position-b.position)})),[theme,kicker]=themeFor(active.title),hasQuiz=active.quizzes.length>0,state=window.EFFAcademyAuth?.state(course.id,active.id)||{reflection:'',quizPassed:false},canFinish=window.EFFAcademyAuth?.ready(course.id,active.id,hasQuiz)||false;root.innerHTML=`<aside class="lesson-nav"><h2>${esc(course.title)}</h2><p>${stats(course).done} of ${course.lessons.length} lessons completed.</p>${groups.map(({m,lessons})=>`<div class="module-label">${esc(m.title)}</div>${lessons.map(l=>`<button class="lesson-link ${l.id===active.id?'active':''} ${complete(course.id,l.id)?'complete':''} ${!unlocked(l)?'locked':''}" data-lesson="${l.id}" ${!unlocked(l)?'disabled':''}><span class="check">${complete(course.id,l.id)?'✓':!unlocked(l)?'🔒':''}</span><span>${esc(l.title)}</span></button>`).join('')}`).join('')}</aside><section class="lesson-panel"><section class="lesson-visual ${theme}"><div><p>VISUAL LEARNING MOMENT</p><h2>${esc(kicker)}</h2><span>EFF Leadership Academy</span></div></section><div class="crumb">${esc((groups.find(g=>g.m.id===active.sectionId)||{}).m?.title||'EFF Leadership Academy')}</div><h1>${esc(active.title)}</h1><article class="lesson-copy"><p>${strip(active.markdown)}</p></article>${active.assets.length?`<aside class="asset-list"><b>Lesson resources</b>${active.assets.map(a=>`<a href="${esc(a.url)}" target="_blank" rel="noopener">${esc(a.name)} ↗</a>`).join('')}</aside>`:''}<section class="assignment-card"><p class="eyebrow">LEADERSHIP ASSIGNMENT</p><h3>Put this lesson into practice.</h3><p>Respond thoughtfully in at least 20 characters, then submit your work for the Academy record. You must submit this assignment before this lesson can be completed.</p><textarea id="assignmentResponse" placeholder="Write your leadership response here…">${esc(state.reflection)}</textarea><div class="assignment-actions"><span id="assignmentStatus">${window.EFFAcademyAuth?.submitted(course.id,active.id)?'✓ Assignment submitted':'Not submitted yet'}</span><button class="done-button" id="submitAssignment">Submit assignment</button></div></section>${active.quizzes.map((quiz,qi)=>`<section class="quiz-card" data-quiz="${qi}"><p class="eyebrow">REQUIRED KNOWLEDGE CHECK</p><h3>Earn 100% to continue.</h3>${(quiz.questions||[]).map((q,i)=>`<div class="quiz-question"><p>${i+1}. ${esc(q.question)}</p>${(q.answers||[]).map(a=>`<label><input type="radio" name="q${qi}-${i}" value="${esc(a)}">${esc(a)}</label>`).join('')}</div>`).join('')}<button class="done-button check-quiz">Check my answers</button><div class="quiz-result" aria-live="polite"></div></section>`).join('')}<div class="lesson-actions"><a href="academy.html">← Back to pathways</a><div><span class="gate-note">${canFinish?'Everything is complete—nice work.':'Complete and submit the assignment'+(hasQuiz?', then earn 100% on the quiz.':'.')}</span><button class="done-button mark-complete" ${canFinish?'':'disabled'}>${complete(course.id,active.id)?'Completed ✓':'Complete lesson'}</button></div></div></section>`;root.querySelectorAll('.lesson-link').forEach(b=>b.onclick=()=>{active=course.lessons.find(l=>l.id===b.dataset.lesson);draw();window.scrollTo(0,0)});root.querySelector('#submitAssignment').onclick=async()=>{const response=root.querySelector('#assignmentResponse').value.trim();if(response.length<20){root.querySelector('#assignmentStatus').textContent='Please write at least 20 characters so your response is meaningful.';return}await window.EFFAcademyAuth?.engage(course.id,active.id,{reflection:response});await window.EFFAcademyAuth?.submit(course.id,active.id,active.title,response);draw()};root.querySelector('.mark-complete').onclick=async()=>{await mark(course.id,active.id);draw()};root.querySelectorAll('.check-quiz').forEach(b=>b.onclick=async()=>{const card=b.closest('.quiz-card'),quiz=active.quizzes[Number(card.dataset.quiz)],questions=quiz.questions||[];let correct=0;questions.forEach((q,i)=>{const answer=card.querySelector(`input[name="q${card.dataset.quiz}-${i}"]:checked`)?.value;if((q.correct_answers||[]).includes(answer))correct++});const passed=correct===questions.length;if(passed)await window.EFFAcademyAuth?.engage(course.id,active.id,{quizPassed:true});card.querySelector('.quiz-result').textContent=passed?'✓ 100%—you cleared this knowledge check.':'You got '+correct+' of '+questions.length+'. Review the lesson and try again; 100% is required.';if(passed)draw()})};draw()})}
  function renderDashboard(){load().then(async({courses})=>{await start();const total=courses.reduce((a,c)=>a+c.lessons.length,0),done=courses.reduce((a,c)=>a+stats(c).done,0),quizzes=courses.reduce((a,c)=>a+c.lessons.reduce((x,l)=>x+l.quizzes.length,0),0);document.querySelector('#dashboard').innerHTML=`<article class="stat"><b>${done}</b><span>lessons completed</span></article><article class="stat"><b>${total?Math.round(done/total*100):0}%</b><span>academy progress</span></article><article class="stat"><b>${quizzes}</b><span>required knowledge checks</span></article>${courses.map(c=>{const s=stats(c);return `<article class="course-card"><div><h2>${esc(c.title)}</h2><p>${s.done} of ${s.total} lessons completed. Continue when you are ready.</p><div class="progress"><span style="width:${s.total?s.done/s.total*100:0}%"></span></div></div><a class="button primary" href="course.html?course=${c.id}">Continue learning →</a></article>`}).join('')}`})}
  return{renderCatalog,renderCourse,renderDashboard};
})();
